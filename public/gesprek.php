<?php

declare(strict_types=1);

/**
 * One conversation, with the clock running and the cost adding up.
 *
 * Empty on purpose. The screen and its stylesheet in
 * assets/css/pages/gesprek.css are ready; the content is yours to build. The
 * message id arrives as ?id=, so start with inputInt('id').
 */

require __DIR__ . '/../bootstrap.php';

requireLogin();

page('Gesprek');

?>
<section class="section">
    <h1>Gesprek</h1>
</section>
